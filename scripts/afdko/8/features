include(../features.all)

feature size {
   parameters  80  # design size (decipoints)
                1  # subfamily identifier
               75  # range start (exclusive, decipoints)
               85; # range end (inclusive, decipoints)
   sizemenuname "MnSymbol";
   sizemenuname 1 "MnSymbol";
} size;

