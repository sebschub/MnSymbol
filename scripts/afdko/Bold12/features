include(../features.all)

feature size {
   parameters 120  # design size (decipoints)
                2  # subfamily identifier
              115  # range start (exclusive, decipoints)
              720; # range end (inclusive, decipoints)
   sizemenuname "MnSymbol";
   sizemenuname 1 "MnSymbol";
} size;

